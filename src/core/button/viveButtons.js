import {Button} from './Button';
import * as COSNT from '../constants';

export const viveLeftTrackpad = new Button(COSNT.VIVE_LEFT_TRACKPAD);
export const viveLeftTrigger = new Button(COSNT.VIVE_LEFT_TRIGGER);

export const viveRightTrackpad = new Button(COSNT.VIVE_RIGHT_TRACKPAD);
export const viveRightTrigger = new Button(COSNT.VIVE_RIGHT_TRIGGER);